# Screenshots

Screenshots for the main README go in this folder. None are included yet.

Wanted, each taken from a clean demo report with generic sample data
(`Calendar[Year]`, `Calendar[Month]`, `Calendar[Date]`), no corporate names or
real data:

| File | Shows |
| --- | --- |
| `single-default-max.png` | Selection mode = Single or All, Default selection = Max, closed on the latest year |
| `multi-dropdown.png` | Selection mode = Multi, Multi display = Dropdown, the open list with ☑ / ☐ labels |
| `multi-list.png` | Selection mode = Multi, Multi display = List, several rows highlighted |
| `format-pane.png` | The Selection card in the format pane |

Keep them under 300 KB each and reference them from the README with relative
paths (`docs/images/<file>.png`).
